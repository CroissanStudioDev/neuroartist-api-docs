---
title: Принять webhook
description: Готовый код приёмника с валидацией подписи
---

import { Tabs, Steps, Callout } from 'nextra/components'

# Приём webhook

Webhook удобнее SSE для серверных пайплайнов: сервис сам повторит доставку при ошибке, а вашему процессу не нужно держать долгое соединение.

## Подключение

<Steps>

### Получите webhook secret

```bash
curl -X POST https://api.neuroartist.ru/me/webhook-secret/rotate -b cookies.txt
# {"secret":"base64-32-byte-secret"}
```

<Callout type="warning">
  Secret показывается **один раз** — сохраните в env-переменные. При компрометации повторите ротацию: старый сразу инвалидируется.
</Callout>

### Поднимите HTTPS endpoint

Любой публичный URL, отвечающий `200 OK`. Локально — туннель через ngrok или cloudflared:

```bash
ngrok http 3000
# Forwarding https://abcd.ngrok-free.app -> http://localhost:3000
```

### Передавайте URL при submit

```bash
curl -X POST 'https://api.neuroartist.ru/queue/fal-ai/flux/dev?webhook=https://abcd.ngrok-free.app/cb' \
  -H "Authorization: Bearer na_live_xxxxxxxxxxxxxxxx" \
  -H "Content-Type: application/json" \
  -d '{"prompt":"…"}'
```

</Steps>

## Код приёмника

<Tabs items={['Hono', 'Next.js Route Handler', 'Express', 'FastAPI']}>
  <Tabs.Tab>

```ts
// hono.ts
import { Hono } from 'hono'
import crypto from 'node:crypto'

const SECRET = process.env.NA_WEBHOOK_SECRET!
const app = new Hono()

app.post('/cb', async (c) => {
  const id = c.req.header('webhook-id')
  const ts = c.req.header('webhook-timestamp')
  const sig = c.req.header('webhook-signature')
  if (!id || !ts || !sig) return c.json({}, 400)

  if (Math.abs(Date.now() / 1000 - Number(ts)) > 300) return c.json({}, 401)

  const raw = await c.req.text()
  const key = Buffer.from(SECRET, 'base64')
  const expected = 'v1,' + crypto.createHmac('sha256', key)
    .update(`${id}.${ts}.${raw}`).digest('base64')

  const valid = sig.split(' ').some(s => {
    try { return crypto.timingSafeEqual(Buffer.from(s), Buffer.from(expected)) }
    catch { return false }
  })
  if (!valid) return c.json({}, 401)

  // Дедуп по webhook-id (24h)
  const seen = await redis.set(`wh:${id}`, '1', { NX: true, EX: 86400 })
  if (seen === null) return c.json({ ok: true, deduped: true })

  const event = JSON.parse(raw)
  if (event.type === 'generation.completed') await onCompleted(event)
  if (event.type === 'generation.failed')    await onFailed(event)

  return c.json({ ok: true })
})
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts
// app/api/cb/route.ts
import { NextRequest, NextResponse } from 'next/server'
import crypto from 'node:crypto'

const SECRET = process.env.NA_WEBHOOK_SECRET!

export async function POST(req: NextRequest) {
  const id = req.headers.get('webhook-id')
  const ts = req.headers.get('webhook-timestamp')
  const sig = req.headers.get('webhook-signature')
  if (!id || !ts || !sig) return NextResponse.json({}, { status: 400 })

  if (Math.abs(Date.now() / 1000 - Number(ts)) > 300) {
    return NextResponse.json({}, { status: 401 })
  }

  const raw = await req.text()
  const key = Buffer.from(SECRET, 'base64')
  const expected = 'v1,' + crypto.createHmac('sha256', key)
    .update(`${id}.${ts}.${raw}`).digest('base64')

  const valid = sig.split(' ').some(s => {
    try { return crypto.timingSafeEqual(Buffer.from(s), Buffer.from(expected)) }
    catch { return false }
  })
  if (!valid) return NextResponse.json({}, { status: 401 })

  const event = JSON.parse(raw)
  // обработайте событие

  return NextResponse.json({ ok: true })
}
```

  </Tabs.Tab>
  <Tabs.Tab>

```js
import express from 'express'
import crypto from 'node:crypto'

const SECRET = process.env.NA_WEBHOOK_SECRET
const app = express()

// Важно: raw body — НЕ express.json
app.post('/cb', express.raw({ type: 'application/json' }), (req, res) => {
  const id = req.header('webhook-id')
  const ts = req.header('webhook-timestamp')
  const sig = req.header('webhook-signature')
  if (!id || !ts || !sig) return res.status(400).end()

  if (Math.abs(Date.now() / 1000 - Number(ts)) > 300) return res.status(401).end()

  const raw = req.body.toString('utf8')
  const key = Buffer.from(SECRET, 'base64')
  const expected = 'v1,' + crypto.createHmac('sha256', key)
    .update(`${id}.${ts}.${raw}`).digest('base64')

  const valid = sig.split(' ').some(s => {
    try { return crypto.timingSafeEqual(Buffer.from(s), Buffer.from(expected)) }
    catch { return false }
  })
  if (!valid) return res.status(401).end()

  const event = JSON.parse(raw)
  res.json({ ok: true })
})
```

  </Tabs.Tab>
  <Tabs.Tab>

```python
import os, hmac, base64, time, json
from fastapi import FastAPI, Request, HTTPException

app = FastAPI()
SECRET = os.environ["NA_WEBHOOK_SECRET"]

@app.post("/cb")
async def cb(req: Request):
    wid = req.headers.get("webhook-id")
    wts = req.headers.get("webhook-timestamp")
    wsig = req.headers.get("webhook-signature")
    if not (wid and wts and wsig):
        raise HTTPException(400)
    if abs(time.time() - int(wts)) > 300:
        raise HTTPException(401)

    raw = (await req.body()).decode()
    key = base64.b64decode(SECRET)
    expected = "v1," + base64.b64encode(
        hmac.new(key, f"{wid}.{wts}.{raw}".encode(), "sha256").digest()
    ).decode()

    if not any(hmac.compare_digest(s, expected) for s in wsig.split(" ")):
        raise HTTPException(401)

    event = json.loads(raw)
    return {"ok": True}
```

  </Tabs.Tab>
</Tabs>

## Тело запроса

```json
{
  "type": "generation.completed",
  "requestId": "req_abc",
  "modelId": "fal-ai/flux/dev",
  "userId": "u_…",
  "completedAt": "…",
  "result": { "images": [{"url":"https://s3.cloud.ru/…"}], "seed": 12345 }
}
```

Для failed:

```json
{
  "type": "generation.failed",
  "requestId": "req_abc",
  "modelId": "…",
  "failedAt": "…",
  "error": { "code": "provider_error", "message": "…" }
}
```

## Чек-лист безопасности

<Callout type="warning">
  **Никогда** не сравнивайте подписи через `===` — это уязвимо к timing-атакам. Используйте `crypto.timingSafeEqual` (Node), `hmac.compare_digest` (Python), `hash_equals` (PHP).
</Callout>

- [ ] Используется raw body (**не** JSON middleware) до валидации.
- [ ] `webhook-timestamp` проверяется на свежесть (≤ 5 минут — защита от replay).
- [ ] Подпись сравнивается через `timingSafeEqual` / `compare_digest` / `hash_equals`.
- [ ] Дедуп по `webhook-id` с TTL 24 часа.
- [ ] Endpoint отвечает `200 OK` за < 30 сек — тяжёлая работа уходит в очередь.
- [ ] При временной ошибке возвращается 5xx — сервис сам повторит до 6 раз.

## Retry-расписание

| Попытка | Задержка |
| --- | --- |
| 2 | 1 минута |
| 3 | 5 минут |
| 4 | 30 минут |
| 5 | 2 часа |
| 6 | 5 часов |

После 6-й — `webhook_dead` в `/me/activity`. Можно перечитать через `GET /queue/.../requests/{id}`.

## Дальше

- [Формат webhooks](/build/webhooks) — заголовки, подпись и тело события.
- [Асинхронная очередь и SSE](/build/async) — как получить `requestId`.
- [`/me/webhook-secret`](/api/me#get-mewebhook-secret) — посмотреть текущий signing secret.
