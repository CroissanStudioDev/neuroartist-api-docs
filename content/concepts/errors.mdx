---
title: Коды ошибок
description: Машинно-читаемые коды и логика ветвления
---

# Коды ошибок

Единый envelope:

```json
{
  "code": "insufficient_balance",
  "message": "Not enough credits",
  "details": { "required": 12, "available": 5 }
}
```

Ветвите по `code` (стабильный machine-readable). `message` — для человека (может меняться). `details` — context-specific.

## Cheat-sheet

| Status | Code | Когда | Что делать |
| --- | --- | --- | --- |
| 400 | `invalid_body` | Не прошёл zod | См. `details.issues` |
| 400 | `invalid_query` | Query-параметр невалиден | См. `details.issues` |
| 400 | `invalid_params` | Path-параметр невалиден | |
| 400 | `invalid_json` | Тело не парсится | Проверьте Content-Type |
| 400 | `minimum_topup` | < `MIN_TOPUP_RUB` | Увеличьте `amountRub` |
| 401 | `unauthorized` | Нет валидного auth | Создайте новый ключ |
| 401 | `invalid_signature` | Подпись webhook невалидна | (Только при приёме webhook от провайдера) |
| 402 | `insufficient_balance` | Кредитов меньше | Top-up; `details: {required, available}` |
| 403 | `forbidden` | Нет прав | |
| 403 | `session_only` | API-ключ заблокирован на этой ручке | Используйте session |
| 404 | `not_found` | Ресурс не существует или не ваш | (IDOR-guard, не различаются) |
| 404 | `unknown_model` | Опечатка в `modelId` | `GET /models` |
| 409 | `conflict` | Уникальный ключ занят | |
| 413 | `asset_too_large` | Файл > `MAX_UPLOAD_BYTES` | Сожмите |
| 413 | `payload_too_large` | Тело > `MAX_BODY_BYTES` | |
| 415 | `unsupported_media_type` | Content-Type не allowlist | |
| 415 | `unsupported_mime` | (Uploads) MIME не allowlist | См. allowed list в [/guides/uploads](/guides/uploads) |
| 429 | `rate_limited` | IP/user/key лимит | `Retry-After` |
| 429 | `key_limit_exceeded` | Месячный лимит ключа | Поднимите через `PATCH /me/keys/{id}` |
| 429 | `too_many_sse_connections` | > `SSE_MAX_CONNECTIONS_PER_USER` | Закройте старые EventSource |
| 500 | `internal_error` | Непредвиденно | Передайте `X-Request-ID` команде |
| 500 | `generation_failed` | Провайдер вернул фатал | Refund автоматический |
| 502 | `provider_error` | Провайдер 5xx | Повторите |
| 502 | `payment_provider` | YooKassa | Повторите |
| 503 | `provider_error` (с `Retry-After`) | Circuit breaker открыт | Подождите и повторите |
| 503 | `storage_disabled` | (Uploads) S3 не настроен | Только в local-dev |
| 504 | `timeout` | Sync `/run` истёк за 5 мин | См. 202 ответ — `requestId` для `/queue` |

## Validation error shape

```json
{
  "code": "invalid_body",
  "message": "Validation failed",
  "details": {
    "issues": [
      { "path": ["prompt"], "message": "Required" },
      { "path": ["num_inference_steps"], "message": "Must be ≤ 50" }
    ]
  }
}
```

## Insufficient balance

```json
{
  "code": "insufficient_balance",
  "message": "Not enough credits",
  "details": { "required": 12, "available": 5 }
}
```

Используйте `details.required - details.available` для UI-сообщения "не хватает 7 ₽".

## Rate limit

```http
HTTP/1.1 429 Too Many Requests
Retry-After: 47
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 0
X-RateLimit-Reset: 1714050300

{"code":"rate_limited","message":"Too many requests"}
```

## X-Request-ID

В каждом ответе:

```
X-Request-ID: 01HXYZ…
```

Передайте этот ID в support — по нему достанем полный лог.
