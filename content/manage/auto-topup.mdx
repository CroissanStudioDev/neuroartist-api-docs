---
title: Авто-пополнение
description: Авто-charge с сохранённой карты при просадке баланса
---

import { Steps, Callout } from 'nextra/components'

# Авто-пополнение

Чтобы не получать `402 insufficient_balance` посреди pipeline.

```
balance < thresholdRub  →  charge amountRub с default-карты  →  webhook  →  +amountRub
```

Срабатывает в момент попытки списания за генерацию. Не чаще раза в 60 секунд.

## Подключение

<Steps>

### Сохраните карту

Единственный способ получить `payment_method_id` — заплатить хотя бы один раз через [`POST /billing/topup`](/manage/billing#пополнение-баланса). YooKassa вернёт его в payment-объекте, сервис сохранит в `payment_method`.

### Сделайте карту default

```bash
curl -X POST https://api.neuroartist.ru/me/payment-methods/pm_abc/default \
  -b cookies.txt
```

Авто-пополнение всегда списывает с default-карты.

### Включите авто-пополнение

```bash
curl -X PATCH https://api.neuroartist.ru/me/auto-topup \
  -H "Content-Type: application/json" -b cookies.txt \
  -d '{"enabled":true,"thresholdRub":200,"amountRub":1000}'
```

| Поле | Допустимый диапазон |
| --- | --- |
| `thresholdRub` | ≥ `AUTO_TOPUP_MIN_THRESHOLD_RUB` (default 10) |
| `amountRub` | от `AUTO_TOPUP_MIN_AMOUNT_RUB` до `AUTO_TOPUP_MAX_AMOUNT_RUB` (default 50 – 10 000) |

</Steps>

<Callout type="info">
  Авто-пополнение **session-only**. API-ключ настроить его не сможет — это намеренно: утёкший ключ не должен включать списания.
</Callout>

## Выключить

```bash
curl -X PATCH https://api.neuroartist.ru/me/auto-topup \
  -H "Content-Type: application/json" -b cookies.txt \
  -d '{"enabled":false}'
```

Backend очищает `thresholdRub` и `amountRub`. Чтобы включить заново, передайте оба значения снова.

## Профили

| Профиль | thresholdRub | amountRub |
| --- | --- | --- |
| Hobby | 50 | 200 |
| Production-сервер | 200 | 1 000 |
| Heavy pipeline (видео) | 500 | 5 000 |

## Если auto-topup упал

(карта отклонена, нет default метода) — обычная ошибка `402 insufficient_balance` на текущем submit. Email + запись `topup_failed` в `/me/activity`.

## Альтернатива — cron

Если нужна более сложная логика (расписание, разные карты, batch-ы) — поднимите свой scheduled job, дёргающий `POST /billing/topup`. Авто-пополнение только реактивное.

## Дальше

- [Биллинг и кредиты](/manage/billing) — как создаётся первый платёж и сохраняется карта.
- [`PATCH /me/auto-topup`](/reference/api/me#patch-meauto-topup) — точная схема запроса.
- [Коды ошибок](/reference/errors) — обработка `insufficient_balance`.
