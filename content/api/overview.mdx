---
title: Обзор
description: Базовые URL, аутентификация, формат ошибок
---

# Обзор

## URL

```
Production   https://api.neuroartist.ru
Staging      https://staging.neuroartist.ru
Local dev    http://localhost:8000
```

## Auth

Один из:

```
Authorization: Bearer na_live_xxxxxxxxxxxxxxxx
x-api-key: na_live_xxxxxxxxxxxxxxxx
Cookie: better-auth.session_token=…
```

Подробнее — [Аутентификация](/concepts/authentication).

## Формат

- Запросы: `Content-Type: application/json` (либо `multipart/form-data` для `/me/uploads`).
- Ответы: `application/json` (кроме SSE и `/metrics`).
- Кодировка: UTF-8.
- Кредиты, суммы — целые числа.
- Даты — ISO 8601 UTC (`2026-04-25T10:00:00Z`).

## Envelope ошибок

```json
{ "code": "insufficient_balance", "message": "Not enough credits", "details": { "required": 12, "available": 5 } }
```

Ветвите по `code` — стабильный machine-readable. Полный список — [Errors](/concepts/errors).

## Стандартные headers

```
X-Request-ID: 01HXYZ…              ← в каждом ответе
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 99
X-RateLimit-Reset: 1714050300
Retry-After: 47                    ← при 429/503
```

## CORS

```
Access-Control-Allow-Origin: <FRONTEND_ORIGINS allowlist>
Access-Control-Allow-Credentials: true
Access-Control-Expose-Headers: X-Request-ID, X-RateLimit-*, Retry-After
```

Cross-origin браузер → `credentials: 'include'` для session cookie.

## OpenAPI

| URL | Что |
| --- | --- |
| `/openapi.json` | OpenAPI 3.1 (EN) — для генерации SDK |
| `/openapi.ru.json` | RU overlay |
| `/docs` | Scalar UI |
| `/llms.txt` | Token-optimized markdown для LLM |
| `/llms-full.txt` | Полная markdown-документация |

В production `/admin/*` и `/webhooks/*` отфильтрованы из спеки.

## Группы ручек

| Префикс | Группа | Назначение |
| --- | --- | --- |
| `/api/auth/*` | Auth | Better Auth — sign-up/in/out, OAuth, ключи |
| `/models/*` | Models | Каталог |
| `/run/{modelId}` | Run | Sync-генерация |
| `/queue/{modelId}/*` | Queue | Async-генерация + SSE |
| `/me/*` | Me | Профиль, ключи, активность, баланс |
| `/billing/*` | Billing | Top-up YooKassa |
| `/webhooks/*` | Webhooks | Server-to-server (провайдеры, YooKassa) |
| `/health`, `/metrics` | Health | Liveness, Prometheus |

## Версионирование

API не версионируется в URL. Изменения — обратно совместимо. Список — `GET /changelog.json`.
