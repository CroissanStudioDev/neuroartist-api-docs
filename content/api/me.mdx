---
title: /me
description: Профиль, баланс, ключи, активность
---

# `/me`

Все эндпоинты требуют аутентификации. Часть — часть — только из дашборда.

## Профиль

### `GET /me`

Текущий пользователь + источник аутентификации.

```json
{
  "user": {
    "id": "u_abc",
    "email": "you@example.com",
    "name": "You",
    "image": null,
    "role": "user",
    "createdAt": "…"
  },
  "auth": { "source": "apiKey", "apiKeyId": "key_abc" }
}
```

`auth.source` — `"session"` либо `"apiKey"`.

### `PATCH /me`

**только из дашборда**

```json
{
  "name": "New name",
  "image": "https://…",
  "lowBalanceEmailOptOut": true
}
```

**Ответ 200:** `{ "user": { … } }`

## Сессии

### `GET /me/sessions`

**только из дашборда**

```json
{
  "sessions": [
    { "id": "sess_abc", "token": "…", "createdAt": "…", "expiresAt": "…", "userAgent": "Mozilla/…" }
  ]
}
```

### `DELETE /me/sessions/{id}`

**только из дашборда**

Ревокирует сессию. Ответ 204.

## Баланс

### `GET /me/balance`

```json
{ "userId": "u_abc", "balance": 248 }
```

### `GET /me/transactions`

**Параметры query:**

| Параметр | Тип |
| --- | --- |
| `type` | `topup` / `deduct` / `refund` / `grant` |
| `limit` | int (default 50) |
| `offset` | int (default 0) |

**Ответ:**

```json
{
  "items": [
    {
      "id": "tx_abc",
      "userId": "u_abc",
      "type": "deduct",
      "amount": -8,
      "description": "flux/dev",
      "createdAt": "…"
    }
  ],
  "total": 142,
  "limit": 50,
  "offset": 0
}
```

`amount` — со знаком: `+` для пополнения и грантов, `−` для списаний.

## Аналитика

### `GET /me/usage`

Снимок расходов по окнам.

**Параметры query:** `windows=5h,24h,7d,30d` (default — все четыре).

```json
{
  "balance": 248,
  "windows": [
    {
      "window": "24h",
      "credits": 156,
      "count": 23,
      "byModel": [ { "modelId": "flux/dev", "credits": 120, "count": 18 } ]
    }
  ]
}
```

### `GET /me/usage/history`

Time-series.

**Параметры query:**

| Параметр | Описание |
| --- | --- |
| `from` | ISO 8601 timestamp |
| `to` | ISO 8601 timestamp |
| `granularity` | `hour` / `day` |

```json
{
  "granularity": "day",
  "from": "2026-04-01T00:00:00Z",
  "to": "2026-04-12T00:00:00Z",
  "series": [ { "bucket": "2026-04-01T00:00:00Z", "credits": 24, "count": 4 } ]
}
```

### `GET /me/usage/by-model`

Расход по моделям за rolling-window.

**Параметры query:** `window=5h|24h|7d|30d`.

```json
{
  "window": "24h",
  "items": [ { "modelId": "flux/dev", "credits": 120, "count": 18 } ]
}
```

### `GET /me/usage/by-key`

Расход по API-ключам (исключает session-расходы).

**Параметры query:** `window=5h|24h|7d|30d`.

```json
{
  "window": "24h",
  "items": [
    {
      "keyId": "key_abc",
      "keyName": "Production server",
      "partialKey": "na_live_xxxx****",
      "credits": 96,
      "count": 14
    }
  ]
}
```

## Активность

### `GET /me/activity`

Лента запросов (submit / status / result / cancel / stream).

**Параметры query:**

| Параметр | Тип |
| --- | --- |
| `modelId` | string |
| `apiKeyId` | string |
| `operation` | `submit` / `status` / `result` / `cancel` / `stream` |
| `status` | `IN_QUEUE` / `IN_PROGRESS` / `COMPLETED` / `FAILED` / `CANCELLED` |
| `limit` | int |
| `offset` | int |

**Ответ:**

```json
{
  "items": [
    {
      "requestId": "req_abc",
      "modelId": "flux/dev",
      "operation": "submit",
      "status": "COMPLETED",
      "credits": 8,
      "durationMs": 4210,
      "createdAt": "…",
      "errorCode": null,
      "metadata": {}
    }
  ],
  "total": 142,
  "limit": 50,
  "offset": 0
}
```

### `GET /me/activity/{requestId}`

Детали одного реквеста + assets.

```json
{
  "record": { "requestId": "req_abc", "modelId": "flux/dev", "operation": "submit", "status": "success", "credits": 8, "durationMs": 4210, "createdAt": "…" },
  "assets": [
    { "assetId": "asset_…", "url": "https://s3.cloud.ru/…", "contentType": "image/webp", "sizeBytes": 487123 }
  ]
}
```

## API-ключи

### `GET /me/keys`

**только из дашборда**

```json
{
  "keys": [
    {
      "id": "key_abc",
      "name": "Production server",
      "prefix": "na_live_",
      "start": "xxxx",
      "expiresAt": "…",
      "createdAt": "…",
      "lifetimeCostRub": 124
    }
  ]
}
```

### `POST /me/keys`

**только из дашборда**

**Тело запроса:**

```json
{ "name": "New key", "expiresIn": 31536000 }
```

**Ответ 201:** содержит `key` **один раз**.

```json
{
  "id": "key_xyz",
  "name": "New key",
  "key": "na_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "prefix": "na_live_",
  "start": "xxxx",
  "expiresAt": "…",
  "createdAt": "…"
}
```

### `PATCH /me/keys/{id}`

**только из дашборда**

**Тело запроса:**

```json
{
  "name": "Renamed",
  "metadata": { "monthlyLimitRub": 5000 }
}
```

`monthlyLimitRub`: `null` снимает лимит, число — потолок расходов на ключ в текущем месяце.

### `DELETE /me/keys/{id}`

**только из дашборда**

Ответ 204.

## Сохранённые карты

### `GET /me/payment-methods`

```json
{
  "methods": [
    { "id": "pm_abc", "mask": "•••• 4242", "type": "bank_card", "default": true, "createdAt": "…" }
  ]
}
```

### `POST /me/payment-methods/{id}/default`

**только из дашборда**

Сделать карту default.

### `DELETE /me/payment-methods/{id}`

**только из дашборда**

Удалить карту. Ответ 204.

## Авто-пополнение

### `PATCH /me/auto-topup`

**только из дашборда**

**Тело запроса:**

```json
{ "enabled": true, "thresholdRub": 200, "amountRub": 1000 }
```

Подробнее — [Гайд](/billing#авто-пополнение).

## Secret для подписи webhook

### `GET /me/webhook-secret`

**только из дашборда**

```json
{ "secret": "base64-32-byte-secret" }
```

Если secret ещё не создан, вернётся `null`.

### `POST /me/webhook-secret/rotate`

**только из дашборда**

```json
{ "secret": "base64-32-byte-secret" }
```

Показывается **один раз**.

## Загрузки

### `POST /me/uploads`

**только из дашборда**

`multipart/form-data`, поле `file`.

**Ответ 201:**

```json
{
  "assetId": "asset_abc",
  "url": "https://s3.cloud.ru/neuroartist/uploads/u_…/asset_abc.jpg",
  "contentType": "image/jpeg",
  "sizeBytes": 487123
}
```

**Ошибки:** `413 payload_too_large`, `415 unsupported_media_type`, `503 asset_storage_disabled`.

См. [Гайд](/build/upload-inputs).

## Дальше

- [Аутентификация](/authentication) — какие операции доступны только из дашборда.
- [Биллинг](/billing) — баланс, платежи и сохранённые карты.
- [Приём webhook](/build/webhooks) — как использовать signing secret.
